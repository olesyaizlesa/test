$(document).ready(function() {/*
  function getRandomInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
  }

  a = []
  a.push $(id)
  var i = 0;
  var j = 0;
  for (i = 0; i < 3; i++) {
    for (j = 0; j < 3; j++) {
    var id = "#" + i + "_" + j;
    var src = "url(img/"+ i + "_" + j +".png)";
    var positionX = getRandomInt(60, 85) + "%";
    var positionY = getRandomInt(20, 75) + "%";
    $(id).css({
      "background": src,
      "top": positionY,
      "left": positionX
    });
    }
  }*/
});
